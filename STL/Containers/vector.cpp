#include <iostream>
#include <vector>
#include <string>

using namespace std ;

void printVector(vector<void> vec){
    for(auto element: vec){
        cout << element << " , " << endl;
    }
}

int main(){
    vector<int> vec;
    // vec.add(10);
    // vec.add(20);
    // vec.add(30);

    return 0;
}